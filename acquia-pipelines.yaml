version: 1.0.0
variables:
  global:
    SLACK_HOSTNAME: "new.slack.com"
    SLACK_CHANNEL: "#acquia"
    PIPELINE_JOB_URL: "https://cloud.acquia.com/app/develop/applications/${PIPELINE_APPLICATION_ID}/pipelines/jobs/${PIPELINE_JOB_ID}"
events:
  build:
    steps:
      - welcome:
          script:
            - echo 'Hello world!'
            - echo $SLACK_CHANNEL
            - echo $SLACK_HOSTNAME
            - sleep 15m
            - echo $PIPELINE_JOB_URL

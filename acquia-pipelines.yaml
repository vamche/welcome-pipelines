version: 1.0.0
variables:
  global:
    PIPELINE_CLOUD_SITE: jamessites

events:
  build:
    steps:
      - welcome:
          script:
            - cat welcome.txt
      - node_v7.10:
          type: script
          script:
            - nvm install v7.10
            - nvm use 7.10
      - script1:
          script:
            - npm install babel-preset-latest
            - echo "TEST ITED DEPLOY"
            - pipelines-deploy
  pr-merged:
    steps:
      - deploy:
          script:
            - echo "Pull request merged"
            - pipelines-deploy

version: 1.2.0

cde-databases:
  - db1
  - db2
  
variables:
  global:
    PIPELINES_DEPLOY_DEBUG: 1
    CDE_DEPLOY_POLL_TIMEOUT: 1400
    CDE_DEPLOY_POLL_INTERVAL: 6
    SYNC_DB_POLL_TIMEOUT: 70
    SYNC_DB_POLL_TIMEOUT: 5

events:
  build:
    steps:
      # If you have build instructions, put them here. They are not required.
      - build_site:
          script:
            - touch docroot/index.html
            - echo "Test!"
            - echo "You successfully deployed your build to an ODE SYNC!.?.:)!?!.?.." > docroot/index.html

  post-deploy:
    steps:
      # Deploy the build artifact to a Cloud on-demand environment.
      - deploy:
          script:
            - date +"%T"
            - pipelines-deploy
            - pipelines-sync-dbs db1 db2
            - date +"%T"

  # When a GitHub pull request is merged, this deletes the coresponding ODE.
  pr-merged:
    steps:
      - deploy:
          script:
            - echo "Pull request merged"
            - pipelines-deploy

  # When a GitHub pull request is closed, this deletes the coresponding ODE.
  pr-closed:
    steps:
      - deploy:
          script:
            - echo "Pull request closed"
            - pipelines-deploy

version: 1.2.0
  
events:
  build:
    steps:
      # If you have build instructions, put them here. They are not required.
      - build_site:
          script:
            - ls -al ~/.ssh/
            - sleep 2m

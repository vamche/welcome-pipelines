version: 1.2.0

variables:
  global:
    ENABLE_FIPS: TRUE
events:
  build:
    steps:
      # If you have build instructions, put them here. They are not required.
      - build_site:
          script:
            - nvm install -s v8.11.1 --enable-fips
            - node --version
